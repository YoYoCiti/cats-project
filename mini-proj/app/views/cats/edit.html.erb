<%= provide(:title, "Edit Cat") %>
<%= provide(:button_text, "Save Changes") %>

<div class="row justify-content-center">
    <div class="col-sm-8" style="margin-top: 10vh;">
        <div class="card text-center" style="border-radius: 15px">
            <div class="card-body">
                <%= render 'form' %>
            </div>
        </div>
    </div>
</div>